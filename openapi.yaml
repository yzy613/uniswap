# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Pool API
    version: 0.0.1
paths:
    /pool:
        post:
            tags:
                - Pool
            operationId: Pool_CreatePool
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/api.swap.CreatePoolRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.swap.CreatePoolReply'
    /pool/{token0}/{token1}/{fee}:
        get:
            tags:
                - Pool
            operationId: Pool_GetPool
            parameters:
                - name: token0
                  in: path
                  required: true
                  schema:
                    type: string
                - name: token1
                  in: path
                  required: true
                  schema:
                    type: string
                - name: fee
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: uint32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/api.swap.GetPoolReply'
components:
    schemas:
        api.swap.CreatePoolReply:
            type: object
            properties: {}
        api.swap.CreatePoolRequest:
            type: object
            properties:
                token0:
                    type: string
                token1:
                    type: string
                fee:
                    type: integer
                    format: uint32
        api.swap.GetPoolReply:
            type: object
            properties: {}
tags:
    - name: Pool
